# Cryptobox 文件格式 v1

所有多字节整数使用大端序。格式常量为 `CRBOXF01`，固定头长度为 256 字节。

## 保险库元数据

`.cryptobox/vault.json` 保存：

- 16 字节随机保险库 ID；
- Argon2id 参数；
- 密码派生主密钥的认证校验值；
- 密钥模式标识 `password-derived-argon2id`。

保险库 ID 同时作为 Argon2id 的公开盐值，并复制在每个加密文件头中。主密钥直接由密码、保险库 ID 和 Argon2id 参数确定，不再生成或保存随机保险库主密钥。默认参数为 64 MiB、3 passes、4 lanes、32 字节输出。密码和主密钥不写入元数据。

`vault.json` 是快速识别和空保险库解锁所需的元数据，但不再是已有加密文件的唯一密钥载体。它丢失后，程序可以从任意完整加密文件头读取保险库 ID，使用密码重新派生主密钥并重建元数据。

## 文件头

固定字段依次为：

| 字段 | 长度 |
| --- | ---: |
| Magic `CRBOXF01` | 8 |
| 格式版本 | 2 |
| 头长度 | 2 |
| Flags | 4 |
| 保险库 ID | 16 |
| 随机文件 ID | 16 |
| 明文总长度 | 8 |
| 分块长度 | 4 |
| 分块数量 | 8 |
| 文件 nonce 前缀 | 4 |
| AES-KW 封装文件密钥 | 40 |
| 保留区域 | 112 |
| HMAC-SHA256 | 32 |

头部 HMAC 密钥由文件密钥、文件 ID 和 HKDF-SHA256 派生。读取时同时验证实际密文长度必须等于 `256 + 明文长度 + 16 × 分块数`。

## 内容块

- 默认明文块为 1 MiB，最后一块可以更短。
- 每块使用同一文件独立密钥执行 ChaCha20-Poly1305。
- 12 字节 nonce 为 4 字节随机文件前缀加 8 字节块序号。
- AAD 包含 magic、版本、保险库 ID、文件 ID、块序号、明文总长度和当前块明文长度。
- 每块密文紧随 16 字节认证标签；因此可直接计算任意块偏移，支持 HTTP Range。

格式升级必须使用新版本号，禁止静默改变 v1 字段或 nonce 构造。

修改密码时，数据块无需解密；程序使用新密码重新派生主密钥，并原子重封装每个文件头中的独立文件密钥。由于主密钥发生变化，索引缓存会自动重建。
