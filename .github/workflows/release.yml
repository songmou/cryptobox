name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
          - os: macos-14
          - os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install
        run: pip install .[dev]
      - name: Test
        run: python -m pytest
      - name: Build with PyInstaller
        run: python -m PyInstaller --clean --noconfirm cryptobox.spec
      - name: Compute binary name
        id: meta
        shell: bash
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          if [[ "$RUNNER_OS" == "Windows" ]]; then BIN="cryptobox-${TAG}.exe"; else BIN="cryptobox-${TAG}"; fi
          echo "bin=$BIN" >> "$GITHUB_OUTPUT"
      - name: Compute SHA256
        shell: bash
        run: >-
          python -c "from pathlib import Path; import hashlib;
          p=Path(r'dist/${{ steps.meta.outputs.bin }}');
          p.with_name(p.name + '.sha256').write_text(hashlib.sha256(p.read_bytes()).hexdigest().lower()
          + '  ' + p.name + '\n', encoding='ascii')"
      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: cryptobox-${{ matrix.os }}
          path: |
            dist/${{ steps.meta.outputs.bin }}
            dist/${{ steps.meta.outputs.bin }}.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
