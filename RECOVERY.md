# 故障与恢复

## 密码错误

密码错误时无法派生出正确的保险库主密钥，程序不会扫描或修改数据。密码不可恢复，也不存在后门。

## `vault.json` 丢失

新建保险库使用密码直接派生模式。只要至少保留一个完整加密文件，程序就能从文件头读取保险库 ID，以原密码重新派生相同主密钥并自动重建 `.cryptobox/vault.json`。错误密码不会创建恢复元数据。

如果保险库从未包含文件，或所有加密文件都已丢失，则没有文件头可供恢复保险库 ID。

## 运行中断

Cryptobox 始终先写同目录 `.cryptobox-tmp-*` 文件，完成同步和验证后才调用原子替换：

- 替换前中断：原明文仍在，临时密文可以删除；
- 替换后中断：原路径已是完整密文；
- 磁盘满、文件锁定或源文件发生变化：临时文件被放弃，原文件保持不变。

重启时内部临时文件不会被当作用户文件。确认没有 Cryptobox 进程运行后，可删除遗留的 `.cryptobox-tmp-*`。

## 完整性错误

Web 中执行“完整校验”会认证每个文件头和数据块。出现错误时：

1. 停止对保险库的外部写入；
2. 保留损坏文件和 `.cryptobox` 目录的副本；
3. 从独立备份恢复对应文件；
4. 不要通过编辑密文、头部或 SQLite 索引尝试修复。

删除 `.cryptobox/index.sqlite3` 只会丢失性能缓存，下次解锁后可以重建。仍建议备份 `vault.json`，尤其是空保险库和早期随机主密钥格式；不要把元数据恢复机制当作文件备份。
